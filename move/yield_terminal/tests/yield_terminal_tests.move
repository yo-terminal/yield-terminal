/*
#[test_only]
module yield_terminal::yield_terminal_tests {
    // uncomment this line to import the module
    // use yield_terminal::yield_terminal;

    const ENotImplemented: u64 = 0;

    #[test]
    fun test_yield_terminal() {
        // pass
    }

    #[test, expected_failure(abort_code = ::yield_terminal::yield_terminal_tests::ENotImplemented)]
    fun test_yield_terminal_fail() {
        abort ENotImplemented
    }
}
*/
